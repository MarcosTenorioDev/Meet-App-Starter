export interface Restaurant {
    id: string
    name: string
    category: string
    deliveryEstimate: string
    rating: number
    imagePath: string
    about: string
    hours: string
}

/* A interface do componente do card dos restaurantes. */